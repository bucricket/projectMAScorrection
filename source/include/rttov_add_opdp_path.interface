Interface
SUBROUTINE rttov_add_opdp_path(opts, opdp_path, opdp_pathA, opdp_pathB)
  USE rttov_types, ONLY : rttov_options, opdp_path_Type
  IMPLICIT NONE
  TYPE(rttov_options),  INTENT(IN)    :: opts
  TYPE(opdp_path_Type), INTENT(INOUT) :: opdp_path
  TYPE(opdp_path_Type), INTENT(IN)    :: opdp_pathA
  TYPE(opdp_path_Type), INTENT(IN)    :: opdp_pathB
End Subroutine
End Interface
